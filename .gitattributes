# Fastembed weights only (not *.lock / *.part under blobs/ — see .gitignore)
models/fastembed/**/blobs/* filter=lfs diff=lfs merge=lfs -text
models/fastembed/**/*.onnx filter=lfs diff=lfs merge=lfs -text
