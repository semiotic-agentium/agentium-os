# Copy to secret-fnox.yaml and fill in real values.
# Do NOT commit secret-fnox.yaml — it is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: fnox-secrets
  namespace: agentium
type: Opaque
stringData:
  OPENROUTER_API_KEY: "REPLACE_ME"
